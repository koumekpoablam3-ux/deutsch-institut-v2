# SQLite en local, PostgreSQL (ex: Neon) en production
DATABASE_URL="file:./dev.db"

# Génère une valeur aléatoire forte : openssl rand -base64 32
NEXTAUTH_SECRET="change-me-in-production"

# URL réelle du site (http://localhost:3000 en local)
NEXTAUTH_URL="http://localhost:3000"
